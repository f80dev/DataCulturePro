import { ScreencutterPipe } from './screencutter.pipe';

describe('ScreencutterPipe', () => {
  it('create an instance', () => {
    const pipe = new ScreencutterPipe();
    expect(pipe).toBeTruthy();
  });
});
